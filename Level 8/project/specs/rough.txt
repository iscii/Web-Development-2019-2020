- menu: for all files in js/pets, display

- getData (tag, type, name)
    - getData(pets, filename.split("_")[0], filename.split("_")[1])

- select a main pet which will be the default pet whenever you log in?


>X - createpet:
    - select an image
        - onclick, set selected = type
    - have a textbox
    - button to submit

    - when created, set currentpet to created pet

