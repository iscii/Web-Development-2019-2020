
- when updating a pet, just update currentPet.

- gray out action buttons when they are not recommended

- change color and image depending on pet's state

>X - createpet:
    - select an image
        - onclick, set selected = type
    - have a textbox
    - button to submit

    - when created, set currentpet to created pet

>X - getData (tag, type, name)
    - getData(pets, filename.split("_")[0], filename.split("_")[1])

>X - menu: for all files in js/pets, display
